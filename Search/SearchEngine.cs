// ============================================
// MODULE 5: Search and Replace
// Part of Group 2: Semantics, Search, History
// ============================================

// TODO: Search for text in current document
// TODO: Support case sensitive/insensitive search
// TODO: Support regular expressions

public class SearchEngine
{
    // Searches for text in document
    public int FindNext(string searchText, int startPosition)
    {
        return -1;
    }

    // Finds all matches
    public int[] FindAllMatches(string searchText)
    {
        return new int[] { };
    }

    // Highlights all matches
    public void HighlightAllMatches(string searchText) { }

    // Set case sensitivity
    public void SetCaseSensitive(bool enabled) { }
}
