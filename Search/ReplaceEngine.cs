// ============================================
// MODULE 5: Search and Replace
// Part of Group 2: Semantics, Search, History
// ============================================

// TODO: Replace single occurrence
// TODO: Replace all occurrences

public class ReplaceEngine
{
    // Replaces current match
    public void ReplaceCurrent(string newText) { }

    // Replaces all matches
    public int ReplaceAll(string searchText, string replaceText)
    {
        return 0;
    }

    // Replaces in all open tabs
    public int ReplaceInAllFiles(string searchText, string replaceText)
    {
        return 0;
    }
}
